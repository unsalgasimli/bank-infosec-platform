global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: aegissec-api
    metrics_path: /api/metrics
    params:
      format: [prometheus]
    static_configs:
      - targets: [app:4000]
