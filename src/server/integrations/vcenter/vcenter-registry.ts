import { VCenterConnector, VCenterRestClient } from './vcenter-connector.js';
import type { VCenterConnectorConfiguration } from '../../../shared/types/vcenter.js';
/** Worker-local: each connector owns a separate REST session cache. */
export class VCenterConnectorRegistry { private readonly runtimes = new Map<string, VCenterConnector>(); public constructor(private readonly createClient: () => VCenterRestClient = () => new VCenterRestClient()) {} public getOrCreate(configuration: VCenterConnectorConfiguration): VCenterConnector { const existing = this.runtimes.get(configuration.connectorId); if (existing) return existing; const runtime = new VCenterConnector(configuration, this.createClient()); this.runtimes.set(configuration.connectorId, runtime); return runtime; } public remove(connectorId: string): boolean { return this.runtimes.delete(connectorId); } public size(): number { return this.runtimes.size; } }
